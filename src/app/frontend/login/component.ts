import { OnInit } from "@angular/core";

enum LoginModes {
  Kubeconfig = 'kubeconfig',
  Basic = 'basic',
  Token = 'token',
}

export class LoginComponent implements OnInit {

  constructor(

  ) {}

  ngOnInit(): void {
    
  }
}